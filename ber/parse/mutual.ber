#parse

type tree = Leaf | Node of tree * tree

> type tree = Leaf | Node of tree * tree








let size = fun t ->
  match t with
  | Leaf -> 1
  | Node (_l, _r) -> 1

> let size = fun t -> match t with | Leaf -> 1
> | Node (_l, _r) -> 1








let depth = fun t ->
  match t with
  | Leaf -> 1
  | Node (_l, _r) -> 1

> let depth = fun t -> match t with | Leaf -> 1
> | Node (_l, _r) -> 1
















