#type
let outer x =
  let id y = y in
  (id x, id true)

> val outer : 'a -> 'a * bool

let capture x =
  let pair y = (x, y) in
  (pair 1, pair true)

> val capture : 'a -> 'a * int * 'a * bool

type 'a list = Nil | Cons of 'a * 'a list

> type declaration

let capture_list x =
  let prepend y = Cons (x, Cons (y, Nil)) in
  (prepend 1, prepend true)

> Type mismatch: int vs bool
> 20 |   (prepend 1, prepend true)
>                            ^^^^ 
