#type
type 'a list = Nil | Cons of 'a * 'a list

> type declaration

let bool_not b =
  match b with
  | true -> false
  | false -> true

> val bool_not : bool -> bool

let bad_fs =
  Cons ((fun (x : int) -> x), Cons (bool_not, Nil))

> Type mismatch:
> 14 |   Cons ((fun (x : int) -> x), Cons (bool_not, Nil))
>                        △△△               ▲▲▲▲▲▲▲▲       
>                                          |
>                                          got: int -> int
>                        |
>                        expected: int
> 
> Got:      bool
>           ▲▲▲▲
> Expected: int
>           △△△
> Reason: variable use

let bad_fs =
  Cons ((fun (x : int) -> (x,x)), Cons (bool_not, Nil))

> Type mismatch:
> 31 |   Cons ((fun (x : int) -> (x,x)), Cons (bool_not, Nil))
>                        △△△                   ▲▲▲▲▲▲▲▲       
>                                              |
>                                              got: int -> int * int
>                        |
>                        expected: int
> 
> Got:      bool
>           ▲▲▲▲
> Expected: int
>           △△△
> Reason: variable use

let bad_fs =
  Cons ((fun x -> (x,x)), Cons (bool_not, Nil))

> Type mismatch:
> 48 |   Cons ((fun x -> (x,x)), Cons (bool_not, Nil))
>                         △△△          ▲▲▲▲▲▲▲▲       
>                                      |
>                                      got: bool -> bool * bool
>                         |
>                         expected: bool * bool
> 
> Got:      bool
>           ▲▲▲▲
> Expected: bool * bool
>                △     
> Reason: variable use

Cons(1, Cons((fun x -> x), Nil))

> Type mismatch:
> 64 | Cons(1, Cons((fun x -> x), Nil))
>           △        ▲▲▲▲▲▲▲▲▲▲        
>                    |
>                    got: int
>           |
>           expected: int
> 
> Got:      'a -> 'b
>              ▲▲   
> Expected: int
>           △△△
> Reason: lambda expression

let bool_not2 b =
  match b with
  | true -> 2
  | false -> 1

> val bool_not2 : bool -> int

let bad_fs =
  Cons ((fun (x) -> x), Cons (bool_not2, Nil))

> Type mismatch:
> 88 |   Cons ((fun (x) -> x), Cons (bool_not2, Nil))
>                                    △△△△△△△△△       
>                                    ▲▲▲▲▲▲▲▲▲       
>                                    |
>                                    got: bool -> bool
>                                    |
>                                    expected: bool -> bool
> 
> Got:      int
>           ▲▲▲
> Expected: bool
>           △△△△
> Reason: variable use

let bool_not3 b =
  match b with
  | true -> false
  | false -> ()

> Type mismatch:
> 107 |   | true -> false
>                   △△△△△
>                   |
>                   expected: bool
> 108 |   | false -> ()
>                    ▲▲
>                    |
>                    got: bool
> 
> Got:      unit
>           ▲▲▲▲
> Expected: bool
>           △△△△
> Reason: tuple expression
