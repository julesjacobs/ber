#type
type 'a list = Nil | Cons of 'a * 'a list

> type declaration

let rec fold_left f acc xs =
  match xs with
  | Nil -> acc
  | Cons (x, xs') -> fold_left f (f acc x) xs'

> val fold_left : ('a -> 'b -> 'a) -> 'a -> list 'b -> 'a

let sum_bool_list xs =
  fold_left (fun (a : int) (b : int) -> a) 0 xs

> val sum_bool_list : list int -> int

let xs = Cons (true, Cons (false, Cons (true, Nil)))

> val xs : list bool

let _ = sum_bool_list xs

> Type mismatch:
> 22 | let _ = sum_bool_list xs
>              △△△△△△△△△△△△△ ▲▲
> 
> Got:      list (bool)
>                 ▲▲▲▲ 
> Expected: list (int)
>                 △△△ 

let add (x : int) (y : int) = x

> val add : int -> int -> int

let lst = Cons(1, Cons(2, Nil))

> val lst : list int

fold_left add 0 lst

> val it : int

fold_left 0 add lst

> Type mismatch:
> 45 | fold_left 0 add lst
>      △△△△△△△△△ ▲        
> 
> Got:      int
>           ▲▲▲
> Expected: 'a -> 'b -> 'a
>              △△         

fold_left 0 add

> Type mismatch:
> 56 | fold_left 0 add
>      △△△△△△△△△ ▲    
> 
> Got:      int
>           ▲▲▲
> Expected: 'a -> 'b -> 'a
>              △△         

fold_left add lst 0

> Type mismatch:
> 67 | fold_left add lst 0
>                △△△ ▲▲▲  
> 
> Got:      list int
>           ▲▲▲▲    
> Expected: int
>           △△△

fold_left add lst

> Type mismatch:
> 78 | fold_left add lst
>                △△△ ▲▲▲
> 
> Got:      list int
>           ▲▲▲▲    
> Expected: int
>           △△△
