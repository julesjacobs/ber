#type
type 'a list = Nil | Cons of 'a * 'a list

> type declaration

let rec fold_left f acc xs =
  match xs with
  | Nil -> acc
  | Cons (x, xs') -> fold_left f (f acc x) xs'

> val fold_left : ('a -> 'b -> 'a) -> 'a -> 'b list -> 'a

let sum_bool_list xs =
  fold_left (fun (a : int) (b : int) -> a) 0 xs

> val sum_bool_list : int list -> int

let xs = Cons (true, Cons (false, Cons (true, Nil)))

> val xs : bool list

let _ = sum_bool_list xs

> Type mismatch:
> 22 | let _ = sum_bool_list xs
>              △△△△△△△△△△△△△ ▲▲
>                            |
>                            got: bool list
>              |
>              expected: int list -> int
> 
> Got:      bool
>           ▲▲▲▲
> Expected: int
>           △△△

let add (x : int) (y : int) = x

> val add : int -> int -> int

let lst = Cons(1, Cons(2, Nil))

> val lst : int list

fold_left add 0 lst

> val it : int

fold_left 0 add lst

> Type mismatch:
> 49 | fold_left 0 add lst
>      △△△△△△△△△ ▲        
>                |
>                got: int
>      |
>      expected: ('a -> 'b -> 'a) -> 'a -> 'b list -> 'a
> 
> Got:      int
>           ▲▲▲
> Expected: 'a -> 'b -> 'a
>              △△         

fold_left 0 add

> Type mismatch:
> 64 | fold_left 0 add
>      △△△△△△△△△ ▲    
>                |
>                got: int
>      |
>      expected: ('a -> 'b -> 'a) -> 'a -> 'b list -> 'a
> 
> Got:      int
>           ▲▲▲
> Expected: 'a -> 'b -> 'a
>              △△         

fold_left add lst 0

> Type mismatch:
> 79 | fold_left add lst 0
>                △△△ ▲▲▲  
>                    |
>                    got: int list
>                |
>                expected: int -> int -> int
> 
> Got:      int list
>               ▲▲▲▲
> Expected: int
>           △△△

fold_left add lst

> Type mismatch:
> 94 | fold_left add lst
>                △△△ ▲▲▲
>                    |
>                    got: int list
>                |
>                expected: int -> int -> int
> 
> Got:      int list
>               ▲▲▲▲
> Expected: int
>           △△△
