#type
let add_pair p =
  match p with
  | (x, y) -> x

> val add_pair : 'a * 'b -> 'a

let _ = add_pair 1 2

> Type error at ber/challenges/ch5_tuple_vs_curried.ber:8:17-8:18: type mismatch: 'b -> 'a * 'c vs int
