#type
let add_pair p =
  match p with
  | (x, y) -> x

> val add_pair : 'a * 'b -> 'a

let _ = add_pair 1 2

> Type mismatch: 'b -> 'a * 'c vs int
> 8 | let _ = add_pair 1 2
>                      ^  
