#type
let add_pair p =
  match p with
  | (x, y) -> x

> val add_pair : 'a * 'b -> 'a

let _ = add_pair 1 2

> Type mismatch:
> 8 | let _ = add_pair 1 2
>             △△△△△△△△ ▲  
> 
> Got:      int
>           ▲▲▲
> Expected: 'b -> 'a * 'c
>                    △   
> Reason: Function argument mismatch
