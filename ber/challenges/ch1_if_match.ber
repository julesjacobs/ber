#type
(* If-branch mismatch inside a let (simulated with match) *)
let f b =
  let x =
    match b with
    | true -> 0
    | false -> false
  in
  x

> Type error at ber/challenges/ch1_if_match.ber:7:15-7:20: type mismatch: int vs bool
