#type
(* If-branch mismatch inside a let (simulated with match) *)
let f b =
  let x =
    match b with
    | true -> 0
    | false -> false
  in
  x

> Type mismatch: int vs bool
> 7 |     | false -> false
>                    ^^^^^
